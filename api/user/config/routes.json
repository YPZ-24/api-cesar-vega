{
  "routes": [
    {
      "method": "GET",
      "path": "/user/paymentMethods",
      "handler": "User.getPaymentMethods",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/user/customerId",
      "handler": "User.createCustomerId",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/user/:id/email/confirm",
      "handler": "User.confirmEmail",
      "config": {
        "policies": []
      }
    }
  ]
}
